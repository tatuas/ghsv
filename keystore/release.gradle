signingConfigs {
    release {
        storeFile file("release.keystore")
        storePassword "password"
        keyAlias "alias"
        keyPassword "keypass"

        v1SigningEnabled true
        v2SigningEnabled true
    }
}
